CREATE TABLE crime_scene_reports (
    id INTEGER,
    year INTEGER,
    month INTEGER,
    day INTEGER,
    street TEXT,
    description TEXT,
    PRIMARY KEY(id)
);


SELECT
description
FROM
crime_scene_reports
WHERE
year = 2020
AND
month = 7
AND
day = 28